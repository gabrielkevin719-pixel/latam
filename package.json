{
  "name": "latam-project",
  "version": "1.0.0",
  "description": "LATAM project with 3-step flow",
  "scripts": {
    "dev": "vercel dev --listen 3000",
    "start": "vercel dev --listen 3000"
  }
}
